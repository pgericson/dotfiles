[user]
    name = Peter Glerup Ericson
    email = pg.ericson@gmail.com

[color]
    diff = auto
    status = auto
    branch = auto

[color "diff"]
    meta = yellow

[alias]
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(blue)%h%C(reset) - %C(green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(yellow)%d%C(reset)' --all

[push]
    default = simple

[core]
    excludesfile = ~/.gitignore
    editor = vim 
    pager = less -q
[pull]
	ff = only
